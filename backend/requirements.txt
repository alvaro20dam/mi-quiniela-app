# Dependencias del backend — versiones verificadas con Python 3.14
Flask==3.1.3
Flask-JWT-Extended==4.7.4
Flask-CORS==6.0.5
bcrypt==5.0.0
psycopg[binary]>=3.1         # psycopg v3 — wheels cp314, reemplaza psycopg2 en Python 3.13+
psycopg-pool>=3.2            # Pool de conexiones (paquete separado en psycopg v3)
python-dotenv>=1.0.1
pytest>=8.2.2
pytest-flask>=1.3.0
gunicorn==22.0.0
requests==2.31.0
