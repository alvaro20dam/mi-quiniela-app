# Archivo .env de ejemplo — NO subir a control de versiones (agregar .env al .gitignore)
FLASK_ENV=development
FLASK_DEBUG=True

SECRET_KEY=cambia-por-un-secreto-aleatorio-largo-y-seguro
JWT_SECRET_KEY=cambia-por-otro-secreto-jwt-independiente
JWT_COOKIE_SECURE=False

DB_HOST=localhost
DB_PORT=5432
DB_NAME=quiniela_db
DB_USER=quiniela_user
DB_PASSWORD=tu_password_segura_aqui

# API Externa para Partidos (football-data.org)
FOOTBALL_API_TOKEN=pon_tu_token_aqui
